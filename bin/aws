#!/usr/bin/env bash

function main {
  local shome="${_aws_home:-"$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"}"
  source "$shome/script/profile"

  exec "$shome/vendor/virtualenv/bin/aws" "$@"
}

source sub-chain "$BASH_SOURCE" "$@"
